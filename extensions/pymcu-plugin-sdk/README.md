# pymcu-plugin-sdk

Shared base classes and plugin protocol for PyMCU backend and toolchain packages.

This package provides the stable API surface that all PyMCU backend and toolchain plugins depend on.
