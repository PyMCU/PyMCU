[project]
name = "traffic-light-pic18"
version = "0.1.0"
dependencies = ["whipsnake-stdlib"]

[tool.whip]
chip = "pic18f45k50"
frequency = 48000000
sources = "src"
entry = "main.py"

[tool.whip.config]
# USB PLL at 48 MHz
