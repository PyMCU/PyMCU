[project]
name = "traffic-light-pic18"
version = "0.1.0"
dependencies = ["pymcu-stdlib"]

[tool.pymcu]
chip = "pic18f45k50"
frequency = 48000000
sources = "src"
entry = "main.py"

[tool.pymcu.config]
# USB PLL at 48 MHz
