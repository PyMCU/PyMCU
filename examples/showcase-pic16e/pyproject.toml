[project]
name = "showcase-pic16e"
version = "0.1.0"
dependencies = ["whipsnake-stdlib"]

[tool.whip]
chip = "pic16f18877"
frequency = 8000000
sources = "src"
entry = "main.py"

[tool.whip.config]
# Internal oscillator at 8 MHz, WDT off
