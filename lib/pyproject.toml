[project]
name = "pymcu-stdlib"
version = "0.1.2a1"
description = "Standard Library for pymcu compiler"
readme = "README.md"
authors = [
    { name = "Iván Montiel Cardona", email = "developer@begeistert.dev" }
]
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/pymcu"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
