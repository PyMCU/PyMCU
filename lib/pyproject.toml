[project]
name = "pymcu-stdlib"
version = "0.1.0a3"
description = "Standard Library for the PyMCU compiler"
readme = "README.md"
authors = [
    { name = "Iván Montiel Cardona", email = "contact@pymcu.org" }
]
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/pymcu"]
artifacts = ["src/pymcu/**/*.inc", "src/pymcu/py.typed"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
